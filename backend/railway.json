{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'PORT=${PORT:-8000} exec uvicorn main:app --host 0.0.0.0 --port $PORT'"
  }
}
