# Copy to .env and fill in your values. NEVER commit .env to git.
# Optional: leave empty to use local embedding fallback and template LLM summary.

TRITON_API_KEY=
TRITON_API_URL=
OPENAI_API_BASE=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4
